<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <Button
        android:id="@+id/submit_button"
        style="@style/ButtonStyle"
        android:layout_width="@dimen/event_detail_submit_button_width"
        android:layout_height="@dimen/event_detail_submit_button_height"
        android:layout_margin="@dimen/event_detail_submit_button_margin"
        android:background="@drawable/selector_common_button"
        android:enabled="false"
        android:text="@string/event_detail_create_new_event"
        android:textColor="@drawable/selector_common_button_text"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</android.support.constraint.ConstraintLayout>